{
	"name": "sports-tracker",
	"version": "1.0.0",
	"private": true,
	"description": "Sports Tracker build config for Docker pipeline",
	"scripts": {
        "dev": "ng serve",
		"build": "vite build"
	}
}
